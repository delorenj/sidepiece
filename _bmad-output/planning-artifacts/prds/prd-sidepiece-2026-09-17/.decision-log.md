# Decision Log — Sidepiece PRD

Canonical memory and audit trail for this run. Every decision, change, and
override is recorded here as the conversation unfolds.

## 2026-09-17

- **Intent: Create.** No existing PRD; `_bmad-output/planning-artifacts/` was empty.
  Workspace bound to `_bmad-output/planning-artifacts/prds/prd-sidepiece-2026-09-17/`.
- **Source of truth: `BRAINDUMP.md`** (rewritten 2026-09-17). Supersedes
  `docs/product-brief.md` (2026-06-04), which is retained as background depth only.
- **Known drift from the June model**, to reconcile during Discovery:
  - Resolution: Traefik-label registry → page advertises `pjid` in `<head>`, `pj info <pjid>` resolves the rest.
  - Annotation: screenshot-only → adds element picker emitting unique CSS selectors.
  - New scope, unrepresented in the June epics: Candystore event feed, agent-session
    list (claude/codex/kimi, reverse-chron), tray settings page with recent projects.
- **Prior art:** Plane board `SIDE` holds 44 backlog tickets (EPIC A–K, created
  2026-06-23) built on the superseded resolution model. Treated as prior art to
  reconcile, not as fixed scope.

### Discovery calibration

- **Stakes: personal tool, single operator.** Jarad is the only user, ever. Rigor
  calibrated accordingly: ~2-4 pages, lean success metrics, no stakeholder /
  approvals / rollout / compliance / ROI clusters. Adapt-In selections limited to
  what a self-hosted personal tool genuinely carries.
- **Working mode: Fast path.** Remaining gaps batched into one consolidated
  question; full PRD drafted with `[ASSUMPTION]` tags on everything inferred,
  then iterated against review.
- **User Journeys: downscaled.** Single operator role, no multi-stakeholder
  handoffs — UJs captured in the lighter one-line shape per template scope dial,
  not full narrative beats.
- **Research:** one subagent grounding the Chrome MV3 capability envelope
  (side panel lifetime, reading `<head>` per navigation, SSE-to-localhost
  survivability, captureVisibleTab limits, stable selector generation).
  Competitive/landscape research deliberately downscoped — no market positioning
  concern on a single-operator tool.

### Consolidated gap round (Fast path)

Answered by user, all four:

- **MVP cut: resolve + chat + tickets.** The complete noticing→acting loop.
  Annotate, event feed, agent-session list, tray settings deferred to §9 —
  specified, not dropped, so epics have a shape to work from.
- **Bridge: local HTTP daemon under `systemd --user`.** Rejected native messaging
  (undebuggable without the extension) and extending an existing 33GOD service
  (release coupling + misplaced filesystem access). Rationale in addendum §A.2/A.3.
  Promoted to a first-class feature (§4.4) rather than an implementation detail,
  because it is a process the user must keep alive.
- **Chat: split stream/dispatch.** Short turns stream inline (FR-5); work
  assignment publishes a Bloodbank command and is acknowledged (FR-6) with
  outcome correlation (FR-7). Rejected streaming-only (June D-epic model, hangs
  on long tasks) and dispatch-only (stops feeling like a cockpit).
- **Agent sessions: list + live status.** Specified in §9 as deferred, with a
  `[NOTE FOR PM]` to promote it if parallel-agent work increases before v1 —
  it is the only pane that prevents an actual collision.

Assumptions taken without asking, all tagged inline and indexed in §13:
pjid is a hard requirement with no URL fallback; origin is irrelevant so the
"distrust localhost" question is dissolved rather than answered; emitting the
declaration is out of scope; element-picker payload carries context not a bare
selector; tickets are read+create only.

### Finding: Bloodbank subject grammar invalidates both source documents

`BRAINDUMP.md` (`bloodbank.repo.<repo-name>`) and `docs/product-brief.md`
(`bloodbank.v1.repo.<repo-name>`) both describe a repo-scoped subject
subscription. Neither is legal: subjects are exactly 5 tokens with no version
and no identity tokens; repo identity lives in `data.repo`. Verified against
`_bmad/custom/workflows/ticket-lifecycle/data/event-schemas.md` and the
scrum-master retirement note, both post-dating the v1 purge (commits 815cb89
through a62acd2).

Consequence: a Project-scoped event view is a payload filter through Candystore,
not a subscription. Reshapes the deferred event feed and FR-7's outcome
correlation. Recorded in PRD §6 and addendum §B.1.

### Finding: `.project.json` rename may have broken a downstream reader

Commit 9dd031e renamed `project_slug` → `project_id`. The ticket-lifecycle event
contract still reads `slug` from `project_slug`. Unrelated to Sidepiece's design
but the Bridge reads the same file — logged as PRD §12 Q5 rather than silently
fixed, since it is not this PRD's call to make.

### Draft written

`prd.md` and `addendum.md` drafted. Essential Spine present. Adapt-In clusters
pulled: Cross-Cutting NFRs, Integration and Dependencies. Added two sections the
menu does not name — §9 Deferred Scope (specified, not built) and §10
Reconciliation with the SIDE Board — because the 44-ticket prior art is a
first-class input to `bmad-create-epics-and-stories` and dropping it would repeat
the June failure. Deliberately omitted: stakeholders/approvals, risk register,
ROI, rollout, data governance, audit trail, compliance, monetization, hardware,
versioning policy, public API surface — none apply to a single-operator tool.

### MV3 capability research folded in

Digest received and reconciled into addendum §C (operative) and §D (what changed).
Five corrections to the first draft:

1. **Panel document persists across tab switches.** Draft assumed per-tab
   `setOptions` overrides; research showed a global panel document survives tab
   switching and per-tab overrides carry a drift failure mode. Relaxed FR-8's
   persistence burden — state only needs to survive the panel *closing*.
2. **SSE vs WebSocket in the service worker.** WebSocket resets the idle timer;
   EventSource has no documented equivalent. Sharpens the "streams live in the
   panel document" rule and shapes FR-7's closed-panel reconciliation.
3. **Host permission scope became a real decision.** A narrow match avoids the
   permission warning entirely — but reintroduces the origin coupling that killed
   the Traefik model. Resolved in PRD §5 in favour of the broad match, valid only
   while the extension stays personally loaded. New assumption, indexed.
4. **Private Network Access.** Absent from the draft. Now a cross-cutting NFR
   plus §12 Q7, because it is the only identified failure mode that can break a
   working install with no change on our side. Mitigation is unconditional so it
   does not block; verification is empirical, not documentary.
5. **The gesture constraint fails silently.** `sidePanel.open()` must be the
   first synchronous call in the handler; an await first no-ops with no error.
   Worth knowing before it is debugged as an intermittent heisenbug.

Also added: SPA navigation needs explicit history observation (a declarative
content script alone does not satisfy FR-1); `captureVisibleTab` is rate-limited
and full-page capture costs the `debugger` permission; selector-generation
library options and their shared failure modes; four prior-art entries, of which
Vercel Toolbar validates the premise and Sourcegraph is the cautionary tale.

Four items moved from §13 Assumptions to a "verified, no longer assumptions"
subsection: service worker termination, gesture requirement, panel scoping and
persistence, captureVisibleTab viewport-only.

### Precision fix: host match scope != SPA re-injection

A second research pass caught imprecise wording in addendum §C. The draft said a
content script "fires automatically on every navigation" and folded the SPA
problem into the host-match bullet. Both wrong in the same way: a content script
injects once per *document load*, and a history-API transition never unloads the
document, so widening the match pattern does nothing for it — match scope governs
*where* a script may inject, not *when* it re-injects.

Now two separate bullets with separate mechanisms. Recommended SPA default is a
`MutationObserver` on `<head>` in the already-injected script — cheapest, no extra
permission — with `history.pushState`/`replaceState` patching as the alternative
(`popstate` alone is insufficient; it does not fire on a `pushState` call) and
`chrome.webNavigation.onHistoryStateUpdated` reserved for when the service worker
itself needs the signal.

Also named the concrete follow-the-tab mechanism: single global panel document +
service worker listening on `tabs.onActivated`/`tabs.onUpdated`, messaging that
document to re-render. Per-tab `setOptions` is for a different product shape
entirely ("different panel per tab, or none on some tabs").

No FR text changed — PRD FR-1 already required explicit history observation.

## Finalize

### Reviewer gate (29 agents, 0 errors)

Decision-log audit + 2 input reconciliations + 5 reviewers (rubric walker,
adversarial, edge-case hunter, fact-check-vs-repo, downstream-consumer), then an
adversarial refuter against every critical/high finding with a default posture of
"the finding is wrong".

21 serious findings raised, **17 refuted**, 4 confirmed. The refutations were
sound — several reviewers had truncated PRD sentences one clause early and
attacked the fragment. Rubric verdict: strong on thesis, scope honesty, shape fit
and downstream usability; adequate on done-ness and strategic coherence.

Independently verified before acting (all confirmed, two worse than reported):

- `agents/hermes/pm/role.yaml` subscribes to `bloodbank.evt.repo.sidepiece.>` and
  `bloodbank.cmd.agent.sidepiece-pm.>` — both embed an identity slug as a subject
  token, which the contract forbids. Live config, not a doc defect.
  `docs/product-brief.md:102` carries a third illegal form with the `v1` token.
- **31 of 56** `.project.json` files under `~/code` have no `ticket_provider.board_id`
  — the majority, not an edge case. The reviewer undercounted at 14/55.
- `agents/hermes/scrum-master/` does not exist; `.project.json` binds it anyway.
- The pjangler registry HTTP service **is listening on `localhost:8764`**, so §12 Q3
  was missing its own leading candidate.
- Exactly one vault item is titled `Plane` (`dlxun2xmwhkt54ns77l4gagrdq`) and
  `op://DeLoSecrets/Plane/apiKey` resolves. Addendum §B.2's duplicate-title/403
  claim was **false** and would have sent FR work to "repair" a working reference.
  Corrected, and the stale claim was removed from agent memory.

### Blocker 1 — thesis: RESOLVED, my framing stands

The braindump's closing image is a dense ambient cockpit ("all in one place");
§1 asserts the opposite ("the value is not any single pane — it is that the
resolution is automatic and correct") and SM-C1 makes panel dwell time a failure
signal. Surfaced as a genuine conflict rather than resolved silently.

**User's call: exact resolution is the thesis.** The cockpit is a means. Dipping
in beats living in it, so SM-C1 stands as written. Recorded here explicitly: the
braindump's density framing was **deliberately overridden**, not lost. `bmad-ux`
should not reintroduce density as a goal on the strength of the braindump alone.

### Blocker 2 — Bridge host: RESOLVED, and it changes the product

The braindump asks for "the cleanest bridge between the Chrome extension and
local Big Chungus paths" — presupposing two machines. The draft collapsed them
into "the workstation" and made loopback-only a hard NFR, foreclosing the split.

**User's call: two machines. Chrome runs on a laptop; the repos and the Bridge
live on big-chungus.** Consequences, all applied:

- Loopback-only is wrong. The Bridge binds to the tailnet interface
  (`burro-salmon.ts.net`) — not loopback, and explicitly not `0.0.0.0`.
- §5's trust boundary is rewritten: the tailnet *is* the authentication boundary
  (WireGuard device auth). No app-level auth scheme — that would be ceremony on a
  single-user tool — but the boundary is now stated rather than assumed absent.
- Latency budgets must absorb network RTT, which is ~1-5ms on-LAN but far worse
  over a DERP relay. Budgets restated as measured over the tailnet.
- §2.2's "single workstation" framing was wrong: single *operator*, multiple
  *devices*. That is not multi-user and does not reopen the portability non-goal.
- EPIC K5 "Remote access (optional)" moves from deferred to v1 in §10.
- The Private Network Access question retargets: the concern is no longer
  loopback but a tailnet address (`100.64.0.0/10`, CGNAT), whose PNA address-space
  classification is even less clearly documented. Q7 rewritten accordingly.

### FR renumbering

Four new FRs required renumbering to keep IDs contiguous. Old → new:
FR-4→FR-5, FR-5→FR-7, FR-6→FR-8, FR-7→FR-9, FR-8→FR-11, FR-9→FR-12, FR-10→FR-13,
FR-11→FR-14, FR-12→FR-15, FR-13→FR-16. New: FR-4 (display resolved identity),
FR-6 (classify a chat turn), FR-10 (page context in turns), and the amended FR-9.
Nothing downstream consumes these yet — no epics have been generated — so the
renumber is safe now and would not have been later.
