{
  "name": "@sidepiece/bridge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup",
    "start": "node dist/bridge.mjs",
    "typecheck": "tsc --noEmit",
    "test": "tsup && node --conditions=source --test \"test/*.test.ts\" \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@sidepiece/contract": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.13.6"
  }
}
